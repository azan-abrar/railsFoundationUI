<!DOCTYPE html>
<!-- paulirish.com/2008/conditional-stylesheets-vs-css-hacks-answer-neither/ -->
<!--[if lt IE 7 ]> <html class="ie6" lang="en"> <![endif]-->
<!--[if IE 7 ]>    <html class="ie7" lang="en"> <![endif]-->
<!--[if IE 8 ]>    <html class="ie8" lang="en"> <![endif]-->
<!--[if (gte IE 9)|!(IE)]><!--> 
<html lang="en" ng-app="confizHRM"> <!--<![endif]-->
<head>
  <meta charset="utf-8" />

  <!-- Uncomment to make IE8 render like IE7 -->
  <!-- <meta http-equiv="X-UA-Compatible" content="IE=7" /> -->

  <!-- Set the viewport width to device width for mobile -->
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <link href='/assets/favicon.ico' rel='shortcut icon' type='image/ico'/>

  <title><%= content_for?(:title) ? yield(:title) : "Confiz HRM" %></title>

  <%= stylesheet_link_tag    "application" %>
  <%= javascript_include_tag "vendor/custom.modernizr" %>
  <%= csrf_meta_tags %>
  <%= javascript_include_tag "application" %>
</head>

<body>

  <div ng-switch="showNavigationBar">
    <nav class="top-bar hide-for-small" ng-switch-when="true">
      <ul class="title-area">
        <li class="name">
          <h1>
            <a href="#/employees">
              <%= image_tag("/assets/confiz-logo.png") %> HRM
            </a>
          </h1>
        </li>
      </ul>
      <section class="top-bar-section">
        <ul class="left">
          <li ng-controller="EmployeesController">
            <form id="searchEmployees">
              <div class="row collapse">
                <div class="small-8 columns">
                  <input type="text" placeholder="Search Here" name="searchField" ng-model="searchField" ui-keydown="{13:'searchEmployees()'}">
                </div>
                <div class="small-4 columns">
                  <a href="javascript:void(0);" class="button" ng-click="searchEmployees()">Search</a>
                </div>
              </div>
            </form>
          </li>
        </ul>
        <ul class="right">
          <% if current_user && current_user.is_admin? %>
          <li class=""><a href="/companies">Companies</a></li>
          <% end %>
          <li class=""><a href="#/employees">Employees</a></li>
          <li class=""><a href="#/departments">Departments</a></li>
          <li class="dark" ng-controller="LoginController">
            <a href="#" ng-click="logout()">Log Out</a>
          </li>
        </ul>
      </section>
    </nav>
  </div>

  <div class="container">
    <div class="row" ng-switch="showNavigationBar">
      <div class="large-12"  ng-switch-when="true">
        <div id="flash" class="alert-box alert" ng-show="flash">
          {{ flash }}
        </div>
      </div>
      <div class="large-6 large-offset-3"  ng-switch-when="false">
        <div id="flash" class="alert-box alert" ng-show="flash">
          {{ flash }}
        </div>
      </div>
    </div>
    <% if current_user && current_user.is_admin? && params[:controller] =~ /companies/i %>
    <%= yield %>
    <% else %>
    <div id="view" ng-view></div>
    <% end %>
  </div>

</body>
</html>
